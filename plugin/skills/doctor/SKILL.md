---
name: doctor
description: Check and repair Epik's setup — gh auth, project initialization, the headless-build workflow, and build secrets — through a detect → offer → fix → report dialogue.
---

Read `doctor.md` in this skill's directory and follow it.

(That file is the canonical doctor. The EpikMCP server vendors it and serves
it as the `setup-epik` MCP prompt, so the same dialogue works on surfaces
that don't load plugins — keep it self-contained.)
